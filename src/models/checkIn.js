class CheckIn {
    constructor(checkedInAt, name, siteCode, id) {
        this.name = name;
        this.siteCode = siteCode;
        this.checkedInAt = checkedInAt;
        this.id = id;
    }
};

export default CheckIn;